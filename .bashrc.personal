#!/usr/bin/env bash
#
# Sourcing order: .profile → .bashrc → .bashrc.personal (this file) → .bash_aliases
#
# Sourced by: .bashrc
# Sources:    (none)
#
# Machine-specific overrides. Add per-machine customizations here
# (env vars, paths, aliases, etc.).
